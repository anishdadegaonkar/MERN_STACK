live server port no - 5500
relative , absolute , sticky , fixed - types of position
text shadow - x horizontal (1st value) y vertical (2nd value)  (3rd value for blur) color (4th value)
box shadow - x y blur blur red
css 2d transform - rotate , scale , screw are the types
translate - it means one place to another place
transform:translate(x , y)
matrix  - we can add screwx , screwy , transform , translate (multiple combinations)
transition - delay , duration , property , timing function

topic name - css transition
@keyframe - animation name , 
assignments - heart shape , cloud , bouncing ball

topic name - canvas svg
we can mix animation , text , draw anything
we will need javascrpt to draw anything
search html canvas on w3 school for practice , HTML media , HTML API's

svg - scalar vector graphics
what is multimedia?

topic name - display grid
justify means - left to right
align means - top to bottom

topic name - media query

topic name - grid
grid-area: row start / col start / row end / col end


template litral - ${}

topic name - Bootstrap4
cdn - content delivery network
we need to add link and script
bootstrap gives 12 equal columns
left right - x
top bottom - y
sr-only - screen only
xs = extra small screens (mobile phones)
sm = small screens (tablets)
md = medium screens (some desktops)
lg = large screens (remaining desktops)
xl - extra large device
prepend - left side
append - right side 
box sizing - border box = it allows you to include padding and border in an elements total width and height

Topic Name - Javascript
typeof - to see data type of variables
document.write - shows output on screen
primitive and nonprimitive - data types
if , else , nested if , function 
= asign
== compare with value
=== compare value with datatype
if we are using return keyword we need to store it somewhere while using function
Parameters - a , b 
argumements - 4, 8 - value of parameters 
ArrowFunction = () => {
    //syntax
}
demo = (x,y) =>{
    return x*y;
}
mul1 = demo(1,3);


() => {
            This is anynomus function
}

callback function - search!
while , do-while , switch  
javascript String Method 
diff between substr , slice , string
var - we can reasssign and redefine in var
let - we cannot change the datatype but we can change value - reasssign
const - we cannot reaasign nor redefine 
why use object - it provides key named index values (id:1001,'name':'john',sal:40000)
array - push , pop , shift , unshift
what is hoisting - call kar ne se phele define karna (function ko)
set - is just like array ([1,2])
map - ([keyname = 'value'],[1,2,23,2],[])
prototype - is used to add new property in object
WHAT IS DOM?   -  each node represents object (it is a tree like structure)
Method Promices = reject and resolve are functions
Method Fetch  = .then .catch
fetch = put your url path in fetch 
resolve = data recived
reject = data not recived
404 range = fetch not avaliable
200 range = successfully excuted


topic name - Node Javascript
node is not a language 
node v8 engine - it runs on it
api - application programimg interface 
it is used to connect db 
written in c,c++ and js 
npm - we can install module
npm init 
npm i express 
express - it is a largest library
to create server - .listen() has two parameters - Port and function
curd - create,update,read,delete 
read doc on - nodejs.org/docs/latest/api/console.html

topic - github
git init , add , push (local to central repository) , commite , rollback , pull (to fetch data)
diff between git (version control system - 2005,open source) and github (runs on cloud - 2008 , have some free storage) 
git init > git add README.md > git status > git config --global user.name > git commite -m "Added README.md File" 

topic name = Socket.io
enables connection between client and server